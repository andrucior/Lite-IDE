# Copy to `.env` and adjust. `.env` is gitignored so real credentials never get committed.
# Consumed by docker-compose.yml for both the postgres service and the backend's DB_* settings.
POSTGRES_USER=liteide
POSTGRES_PASSWORD=liteide
POSTGRES_DB=liteide
